{
  "name": "api",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "versions": "wrangler versions upload",
    "lint": "oxlint --deny warnings",
    "check-types": "wrangler types --env-interface CloudflareBindings && tsc --noEmit",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "dependencies": {
    "@repo/data": "workspace:*",
    "hono": "^4.12.2"
  },
  "devDependencies": {
    "@repo/typescript-config": "workspace:*",
    "wrangler": "^4.67.0"
  }
}
