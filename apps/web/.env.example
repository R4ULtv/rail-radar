NEXT_PUBLIC_API_URL="http://localhost:8787" # The base endpoint for the backend API
NEXT_PUBLIC_MAPBOX_TOKEN="" # Public Mapbox access token required to initialize map components and load map tiles
MAPBOX_SERVER_TOKEN="" # Server-side Mapbox token used to render Open Graph map images
